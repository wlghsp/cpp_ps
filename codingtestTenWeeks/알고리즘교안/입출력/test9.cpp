#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++)
    {
        cin >> s;
    }

    cout << s << "\n";
    return 0;
}